# Release, CLI, configuration and architecture audit

Audit source: the complete `SPEC.md` (SHA-256 `6598170acd19ce38c166cd2dd2b44587002ca0d9a8acff8bad107057203c3134`), independently compared with the user's original `/Users/james/Downloads/SPEC.md`; the bytes match. This audit retains the stable IDs from `docs/coverage.md` and supplies evidence for the primary agent's final reconciliation. It does not redefine an external validation requirement as an implementation pass.

**Frozen implementation, local checks and complete archive/install verification pass.** The implementation and lockfile are committed at `d9284e80f46836efc48365b7fe3a3d038d7e771f`; the frozen build-input identity is `31586c3bc81633d5d3f912b7e717800caf185ecfce5358f7feeb92b3aa8129e3`. The current repository was inspected directly rather than inferred from earlier completion messages. Final distribution byte attestations belong in `dist/`, outside the archives whose hashes they record. Native Linux/Windows runtime, real emulator appearance/input and controlled reference hardware remain separate required evidence.

## Evidence reviewed or executed

- **RA-CLI:** Executed `cargo test --locked --offline --test cli_acceptance`: **23 passed**, zero failed/ignored. The configuration-independent command fixture runs nine combinations of `config path`, `man`, and completions with interactive-only options, all rejecting before configuration or terminal access. The classification fixture independently filters standard/practice/paste-attempted/assisted-code records across history, statistics and all three exports; it also intersects classification with completion outcome. Invalid classification values fail before storage access. Inspected the complete `src/cli.rs`, `src/main.rs` and executable fixtures.
- **RA-SET:** Executed `cargo test --locked --offline --test settings_acceptance`: **20 passed**, zero failed/ignored. Inspected `src/config.rs`, the setting registry, palette construction and transactional application path.
- **RA-MSRV:** The final frozen-source Rust 1.95 and MSRV 1.88 all-target/all-feature runs each pass **253 tests, zero failures, one explicitly ignored 100,000-row release benchmark**, across 18 target executables. Exact commands, compiler/source/test hashes and complete logs are in `docs/measurements/stage-d-frozen-rust-validation.json`. The separate Windows GNU all-target Rust 1.88 check also passes and compiles native privacy tests; it does not execute Windows code.
- **RA-PACK-TEST:** Executed `python3 scripts/release_test.py`: **17 passed**, with one explicitly skipped native Windows junction fixture. Coverage includes archive determinism, original manifest retention, outer and payload checksum rejection before extraction, global/target Rust and C CPU flags, installation idempotence and rollback, ownership-safe uninstall, modified files, traversal/duplicate entries, symlink parents, foreign OS selection, incompatible processor refusal before prefix writes, retained Rosetta compatibility, mocked Windows reparse attributes, preservation below reparse points and native executable headers. Python compilation checks passed for all release scripts.
- **RA-DEPS:** `python3 scripts/licenses.py --check` reproduces **187 dependency entries and 363 notice/source files** for 188 total locked packages. Both cargo-deny 0.20.2 (advisories/bans/licenses/sources) and cargo-audit 0.22.2 pass against the current lockfile and previously refreshed RustSec commit `5a0ebedfe8bdd2e295b171f4162f8c977bcad9a5`, with no ignored advisories. `release-permissions-dependency-check.json`, `cargo-audit-permissions-metadata.json`, and `dependency-integrity-permissions.json` record exact identities; all 185 registry archives match their lockfile checksums. The only added package is the reviewed local Windows adapter.
- **RA-CI:** Parsed `.github/workflows/ci.yml` with PyYAML; inspected read-only permissions, immutable action pins, credential suppression, quality/MSRV/five-native-target/source jobs and actual script options. Native jobs validate their packaged executable and isolated install/uninstall. Source jobs validate original source archives. This validates the workflow definition, not remote execution.
- **RA-PRODUCT:** Fresh `product-permissions-final.json`, `pty-permissions-final.json`, `transports-permissions-final.json` and their Rosetta counterparts each pass **25 product, 27 lifecycle and 10 actual tmux/isolated loopback-SSH cases**, with zero failures/skips. The native ARM64 and translated x86-64 hook binary hashes are explicitly bound to frozen source 31586c3b…8129e3. The exact production ARM snapshot also completes start/type/JSON/save/cleanup under verified IP denial (`network-denied-permissions-final.json`). Synthetic PTYs and loopback transports do not establish real-emulator appearance or remote latency; previous source reports remain preserved.
- **RA-BUILD:** Rebuilt all five production binaries from unchanged frozen source `31586c3b…8129e3`. `docs/measurements/release-permissions-builds.json` records every command, profile, compiler, source/binary hash, byte count and actual header/import inspection. Both Linux targets require GLIBC symbols no newer than 2.17; Windows imports only system/UCRT libraries, including the adapter's Advapi32; macOS records deployment 11.0. Exact native ARM64 and Rosetta x86-64 production binaries each pass nine isolated commands without creating a missing data directory (`release-permissions-commands.json`). Original pre-privacy-freeze reports remain preserved under their original names.
- **RA-LINT:** Frozen-source all-target/all-feature Clippy with `-D warnings` passes for native ARM64 and all four foreign configurations (macOS x86-64, Linux x86-64/arm64, Windows GNU). `release-permissions-clippy.json` and per-target logs record zero warning lines and exact source/compiler identity. The Windows helper and application native ACL fixtures are included through workspace defaults; full Windows GNU Rust 1.88 compilation also passes (`windows-privacy-compilation.json`). Native Windows MSVC/runtime evidence remains external.
- **RA-WIN-PERM:** The actual inherited-DACL implementation gap is closed: atomic protected TokenUser file/directory creation, original/resolved path validation, guarded private SQLite parent and existing DB/sidecar ACL checks, config/export/import/recovery integration and reviewed default-owner handling. Eight adversarial helper tests and application Windows ACL fixtures compile warning-free. The helper test executable additionally links successfully for Windows GNU (`windows-privacy-test-link.json`); no Windows test execution is inferred. Independent review records exact helper hashes and local check logs in `audit-windows-permissions-review.md`. Native standard/elevated Windows execution, ACL-incapable and UNC filesystem checks remain external validation, not an unimplemented feature.
- **RA-ARCHIVE:** The complete six-archive draft passes actual validation in `docs/measurements/release-draft-validation.json`, with full-input provenance in `release-draft-provenance.json` and separate byte/packaged-CLI inspection in `release-draft-independent-review.json`. All five payload binaries exactly match RA-BUILD; native policy/license docs, man and five completions are present. The source archive retains the original Cargo/workspace/patch/lock and the entire helper. Both exact macOS packages are installed, executed, idempotently reinstalled and uninstalled inside disposable prefixes while preserving user-data sentinels; Linux/Windows execution/install remain explicitly unperformed. Draft archives remain in `target/release-preview-permissions`. Final distribution uses the same executables and committed final documentation, with byte attestations outside its archives.
- **RA-ARCH:** Inspected the pure reducer, immutable models, custom Ratatui viewport, input queue/epochs, adapter loop, deferred setting changes and storage facade. This is source evidence, augmented by the named deterministic tests and actual PTY reports; it is not a timing claim.

The three pinned Actions were independently resolved from their official release pages to their complete commit IDs: [checkout v7.0.1](https://github.com/actions/checkout/commit/3d3c42e5aac5ba805825da76410c181273ba90b1), [setup-python v6.3.0](https://github.com/actions/setup-python/commit/ece7cb06caefa5fff74198d8649806c4678c61a1), and [upload-artifact v7.0.1](https://github.com/actions/upload-artifact/commit/043fb46d1a93c77aae656e7c1c64a875d1fc6a0a). The five selected runner labels and architectures are present in the [official GitHub runner reference](https://docs.github.com/en/actions/reference/runners/github-hosted-runners). These facts were checked during this audit; a YAML file does not demonstrate an executed remote job.

## Product and global requirements

| ID | Current conclusion and concrete evidence |
|---|---|
| P-001 | Implemented. Bare launch prepares the real source before terminal setup; the first usable frame is input-ready. `first_input_without_ready_wait` and `first_key_focus_json` exercise actual application startup. |
| P-002 | Implemented. Ready/Running/Results and restart use one engine; palette/history/review are overlays outside active typing. Product `palette_compact_abort`, `missed_practice_return`, and lifecycle `restart_epoch` pass on the recorded build. |
| P-003 | Implemented and locally validated by first-input coordinate/count assertions and immutable geometry snapshots. Actual visual comfort is an emulator check. |
| P-004 | Stage order and evidence are retained in the central ledger. Input/score boundary regressions were repaired before release packaging. This audit does not infer a subjective priority claim from feature count. |
| P-005 | Local, standalone Rust executable is implemented without account/browser/service/network dependencies. RA-BUILD rebuilt all five required machine-code targets; native macOS ARM64 and translated macOS x86-64 commands pass. Native Linux/Windows runtime remains an external gate. |
| P-006 | README and CLI use independent `clack` branding and explicitly avoid score-for-score compatibility claims. Themes/data are independently authored or licensed, with notices. |
| P-007 | Shared command bindings retain baseline Ctrl/Esc controls before associated text. Protocol/PTY tests exercise baseline and optional enhanced decoding. README/terminal matrix explicitly bound IME/physical-key claims; actual emulator coverage remains external. |
| P-008 | Each named 1.0 feature is implemented through the shared engine/configuration/storage adapters. Modes are covered by sample/kernel fixtures; settings/practice/history/review/PB/recovery by RA-PRODUCT; exports/imports by RA-CLI. Final feature-specific rows remain the authoritative detailed audit. |
| P-009 | Source and dependency inspection finds no excluded online/social/AI/keyboard-diagram/audio/editor/web-companion feature. No runtime subprocess invocation, clipboard or networking API exists in application source. |
| P-010 | Custom/import input validation rejects controls, bidi controls and unsupported shaping. Documentation states composition/IME/font limits and makes no universal UTF-8 language guarantee. |

## Configuration registry and persistence

| ID | Current conclusion and concrete evidence |
|---|---|
| CFG-001 | Implemented. Typed config and `settings::REGISTRY` cover every serialized setting; palette builds entries from that registry. RA-SET exhaustive leaf coverage and product palette/editor tests pass. |
| CFG-002 | Test mode, limits, source/pack, modifiers, policy and completion have registry entries, typed serialization and CLI/sample tests. |
| CFG-003 | Theme, focus, width, alignment, rows, spacing, error/typed styling and caret have independent fields and range checks. Product theme edit/preview and snapshot matrices exercise application. |
| CFG-004 | Progress, WPM and accuracy booleans are independent; speed units select WPM/CPM. Canonical defaults and registry enumeration tests prevent drift. Active status rendering is additionally checked by the UI owner. |
| CFG-005 | Difficulty/backspace/stop/blind/minimum speed/accuracy use registry validation and engine cross-field constraints; invalid combined edits are transactional. |
| CFG-006 | Pace off/fixed/PB, code auto-indent and missed/slow selection are typed independent settings. `personal_best_pace_current_profile_uses_only_the_matching_indexed_best` plus product pace/slow-selection cases pass. |
| CFG-007 | Named presets, favorites, binding aliases and detail defaults are data in the registry. Safe bindings and saved presets have RA-SET coverage; product tests exercise real reader reconfiguration. |
| CFG-008 | All four privacy controls are present. Private sessions lock suppression through later preset/settings changes; product `private_preset_lock` and `private_trace_opt_in_suppression` validate real workflows. |
| CFG-009 | Timer-only canonical defaults and the shipped focused preset hide other status. Training metrics and high-contrast themes are independent choices. Snapshot/PTY evidence is local; actual-emulator readability remains external. |
| CFG-010 | Exact canonical schema-version-1 defaults are loaded by `canonical_spec_configuration_loads_and_unknown_paths_are_precise`; exhaustive registry/default test passes. |
| CFG-011 | Config default/file/preset/CLI precedence is tested in pure configuration and actual processes; later explicit UI edits have product coverage. |
| CFG-012 | CLI flags do not persist. `persist_edits` changes only explicit fields, while preset application persists its explicitly selected data. Comment-preservation/CLI-override tests and product `theme_preview_field_persistence` pass. |
| CFG-013 | Save-default intentionally serializes the effective configuration. API tests and real `shipped_presets_save_defaults` case pass. |
| CFG-014 | Registry owns setting kinds, serialization paths, labels/help, effective defaults and validation; palette, CLI drift tests and explicit Edit parsing share it. No parallel scoring model is introduced by a preset. |
| CFG-015 | Comment-preserving TOML, private temporary creation, sync and rename implement atomic writes. RA-SET checks I/O failure, symlink refusal, invalid baseline and Unix permissions. Windows now validates the final namespace and creates protected user-owned temporary files before payload; two native Windows fixtures cover repeat replacement/comment retention and ADS refusal. Cross-compilation passes; native Windows filesystem execution remains external. |
| CFG-016 | Unknown paths/values and incompatible fields produce explicit diagnostics; transactional edits avoid silent reinterpretation. RA-SET and actual CLI argument/filter/import errors pass. |
| CFG-017 | Explicit broken configs are rejected before raw mode without rewriting, including syntax/schema/unknown/range/contradiction/UTF-8 PTY cases. Automatic-load source branch reports and uses defaults while preserving the original; it does not overwrite a live user's configuration during testing. |
| CFG-018 | Default/focused/code presets are non-executable data; saved custom presets only contain validated settings. Unit and real shipped-preset cases pass. |

## CLI and stream contract

| ID | Current conclusion and concrete evidence |
|---|---|
| CLI-001 | All illustrative launch options exist in Clap and sample preparation; RA-CLI tests accepted combinations and ordinary bare startup has PTY evidence. |
| CLI-002 | Mutually exclusive sources/mode flags and bounded values reject with exit 2 before setup. RA-CLI, invalid-source/config/argument PTY cases and new configuration-independent command boundary regression pass. |
| CLI-003 | `--json` requires `--once` interactively. Recorded completion/challenge-failure PTY cases exit after one result; commands reject interactive-only options. |
| CLI-004 | Stdout uses one fallible versioned serializer after terminal restoration. PTY `one_json` rejects escape bytes, trailing objects and nonfinite constants; interrupted/deadline/stdin cases pass. |
| CLI-005 | Final actual tests distinguish complete/challenge-failed exit 0, runtime failure 1, invalid argument 2 and explicit interruption 130. Final adapter-overload verdict and lifecycle reruns preserve one honest public result after terminal restoration. |
| CLI-006 | Controlling input/output is acquired before bounded stdin-source consumption. Detached missing-terminal and redirected-source PTY cases validate separation and preservation. |
| CLI-007 | Unix implementation is locally exercised. Windows CONIN$/CONOUT$ path and exact-mode restoration are implemented and cross-checked; native Windows runtime remains external. |
| CLI-008 | Noninteractive command subprocess fixtures run without a terminal and do not create missing history. Missing interactive TTY returns a neutral error with unread stdin preserved. |
| CLI-009 | History pagination/filters, exact current-profile weighted statistics and JSON/JSONL/CSV exports use read-only indexed storage APIs; RA-CLI exercises real SQLite fixtures and empty/corrupt stores. Classification is orthogonal to outcome, including explicit complete/practice and interrupted/standard intersections; JSONL/CSV use the same storage predicate. |
| CLI-010 | Config path/show/resolved/validate, language list/bounded atomic import, theme list and doctor are implemented and exercised as executable commands. Imports reject scripts/shadowing/conflicts and preserve existing data. |
| CLI-011 | Doctor reports observed geometry, configuration, backend and explicit unknown capabilities. Only `--probe` performs bounded queries; recorded product probe case validates response flags and restoration. |
| CLI-012 | Baseline mode is default; enhanced reporting is opt-in. Shared binding/protocol tests and session guard restoration are implemented. Native supported-emulator execution remains an external gate. |
| CLI-013 | Man/help and five shell completions are generated from `Cli::command`. RA-CLI tests broken-config accessibility and output errors; final archive verification compares packaged output with the actual executable. |
| CLI-014 | Central export serialization requires explicit include-text plus actual retained text. Default/opt-in/unavailable text, all three formats and private new-only output files have actual executable fixtures. |

## Architecture and scheduling

| ID | Current conclusion and concrete evidence |
|---|---|
| ARCH-001 | Stable Rust, MSRV1.88 and compatible Ratatui0.30.2/Crossterm0.29 are selected and tested. Cargo.lock SHA is eb7f81f39fe4ba6ccfb6f86e7e2610fe55800edd42ee93297d97fcbaa351e464. The complete implementation and lockfile are committed locally at d9284e80f46836efc48365b7fe3a3d038d7e771f. |
| ARCH-002 | Custom `TypingWidget` and cached `Viewport` use Ratatui's full-frame backend, not a replacement terminal renderer. UI snapshot and actual PTY cell assertions validate composition. |
| ARCH-003 | Reviewed graph and source use bundled Rusqlite/SQLite, typed Serde/TOML, pinned Unicode libraries, versioned SplitMix64 and platform directories. |
| ARCH-004 | One core library plus binary; application forbids unsafe. No Tokio/ECS/DI/server is introduced. The small Windows-only `clack-private-fs` workspace member is the demonstrated PRIV-005 architecture exception, with safe public APIs, isolated reviewed Win32 calls, RAII and bounds proofs in `vendor/clack-private-fs/SAFETY.md`; independent review is in `docs/audit-windows-permissions-review.md`. The reviewed Crossterm fork and upstream dependency internals remain separately identified. |
| ARCH-005 | Reducer imports no terminal/SQL/filesystem/live clock; `apply(action, receipt_us)` and deterministic replay tests establish injected timing. |
| ARCH-006 | UI borrows immutable engine/result models, mutating only presentation buffers/cache. Score changes occur in the engine owner. |
| ARCH-007 | Target/entered `Unit` buffers are separate; stable token starts, correction deltas and logical viewport are explicit. Escapes are terminal output only. Correction/Unicode/layout tests cover these boundaries. |
| ARCH-008 | One input reader, one engine/render owner, one storage worker. Storage startup occurs only after the first safe, complete usable frame and later reconfigures the same worker. |
| ARCH-009 | Reader exclusively owns normal Crossterm polling/reading. Doctor probe is a separate noninteractive command; no EventStream or simultaneous event consumer exists. |
| ARCH-010 | Small envelopes carry receipt time, sequence and epoch. Engine/output stay on owner thread; storage takes immutable records and cannot read keys or write terminal output. |
| ARCH-011 | Restart/disarm acknowledgements close prior epochs and clear old decoded input. Actual restarted random targets and associated/release events have PTY regressions. |
| ARCH-012 | Ordered reader watermarks and closing-epoch integrity verdict precede public/persisted completion. Boundary/overload fixtures explicitly prevent late queue overload from yielding a valid record. |
| ARCH-013 | Queue capacity is exactly 4096; saturation sets sticky overload and interrupts/invalidate eligibility. Critical lifecycle packets remain deliverable and sequence gaps are observable. |
| ARCH-014 | Queue coalesces resize notices while retaining earliest unsafe geometry and all text order. Deterministic resize/queue tests and stress reports exercise the contract. |
| ARCH-015 | Owner parks until input/worker/visible status/deadline; unpark tokens prevent lost wakes. Earlier 60-second Ready/Results observations emitted zero bytes with CPU below accounting resolution. Final storage-enabled measurement belongs to performance audit. |
| ARCH-016 | Owner drains at most 64 envelopes or 1 ms, then services due work. Reader timestamps determine scoring independently of drawing. Source bounds and actual stress order are separate evidence. |
| ARCH-017 | Fixed and independently reviewed at source freeze. `Presentation::same_text` compares constant-size state/caret/token/tail data; ignored Ready keys, prohibited edits and hidden rejected errors request no frame. Scheduled comparisons use rounded visible values and exclude off-viewport pace. Unit regressions cover ignored actions, provisional Unicode, hidden/visible rejected attempts and display precision. Actual frame-count checks belong to the final terminal report. |
| ARCH-018 | Fixed and independently reviewed at source freeze. Presentation-owned `LiveCache` retains metric samples for 250 ms and pace samples for 100 ms, including frequent text-driven frames; the scoring state remains immediate. Rapid-text regression verifies both maximum update cadences. The progress label changes at 1 Hz, and Ready/Results have no default interval. Ordered reader deadlines remain independent of all visible metrics. |
| ARCH-019 | Each dirty draw describes the whole Ratatui frame; backend diff emits changed cells. Normal keys do not clear the terminal. Query-free suspend reset has PTY regression coverage. |
| ARCH-020 | Unit widths and token start/layout revisions are cached; viewport invalidation begins at affected token or geometry/source change. Ordinary-correct-input cache test and new-epoch reset tests validate reuse. |
| ARCH-021 | Correctness never requires synchronized output; session has a baseline coherent flush path. PTY decoder checks no synchronized-output state remains after cleanup. |
| ARCH-022 | Ordinary text input path has no I/O/SQL/file/pack parsing or transcript-wide diff. Source changes, exports and settings are deferred outside the bounded input batch; storage queries/commits stay in the worker. Final large-practice preparation audit is owned by the primary agent. |
| ARCH-023 | Incremental counters/preallocated unit buffers avoid whole-history scoring. Warm ASCII allocation test and measured reducer harness separately verify the zero-allocation target. Final release performance rerun is required after engine changes. |
| ARCH-024 | Finite source/input bounds, 4096-unit Zen edit window, 3601 samples and optional bounded event traces are explicit. Private/default trace suppression and long-window cycling have tests/reports; storage owner audits shared metadata and legal maximum inputs. |

## CI, deliverables and final gates

| ID | Current conclusion and concrete evidence |
|---|---|
| CI-001 | Formatting gate exists; integrated `cargo fmt --all -- --check` passes, against the final frozen workspace source. |
| CI-002 | CI runs warning-free all-target/all-feature Clippy on every native matrix configuration. RA-LINT passes native ARM64 and all four foreign compilation configurations locally; no remote/native foreign-OS or MSVC lint execution is claimed. |
| CI-003 | CI defines unit/property/snapshot/integration gates. Frozen current compiler and Rust 1.88 each pass 253 tests (RA-MSRV); the explicit release 100,000-row benchmark is separately recorded by the performance owner. Native/translated PTY and transport evidence is tied to its exact binary/source identity (RA-PRODUCT). |
| CI-004 | Five native release jobs plus source archive job define production packages and isolated installation checks. RA-ARCHIVE and the independent draft review validate the complete six-archive collection against frozen binaries, including actual host installation. Remote native CI execution remains external. |
| CI-005 | Reproducible notices, immutable lockfile, source/license restrictions and no ignored advisories are enforced. RA-DEPS passes after actual advisory refresh. |
| CI-006 | No noisy shared-runner absolute timing gate is misrepresented as controlled hardware. A dedicated-machine baseline/10% regression gate remains externally required. |
| SHIP-001 | The complete source repository and original source archive preserve Cargo manifests/lockfile, application, tests, scripts, approved data, vendor fork/privacy adapter, notices, docs, CI and source specification. Actual archive verification and independent758-input draft inspection pass; implementation and lockfile are committed at d9284e80f46836efc48365b7fe3a3d038d7e771f. Final collection byte identities are recorded in the distribution sidecars. |
| SHIP-002 | Config schema 1, record schema 1, SQLite migration version and separately versioned export envelope are explicit and tested. Source-format validation and old/unknown versions are rejected as documented. |
| SHIP-003 | Data source/license/Unicode manifests and full dependency notices are present; RA-DEPS verifies exact supplied license files, including unchanged MPL source. Content audit owns pack/source license approval details. |
| SHIP-004 | Automated tests, PTY/product/transport harnesses, reducer/render/terminal/storage benchmarks and package/installer verifier are supplied. Harness execution claims retain their own fixture/binary identity. |
| SHIP-005 | Actual measurement reports and methodology distinguish initial pre-storage versus final release, warm versus cold, synthetic PTY versus emulator and uncontrolled versus controlled hardware. Final performance report is owned by the benchmark agent. |
| SHIP-006 | Terminal matrix and current reports are supplied. Final native ARM64 and Rosetta x86-64 lifecycle/product and actual tmux/loopback-SSH cases all pass, with earlier fixture failures retained and explained by the terminal owner. Actual graphical-emulator/native foreign-OS checks remain external. |
| SHIP-007 | Installable packages exist for all five required targets, with portable production binaries below15MiB, exact build identities and inspected ABI/import requirements. The complete six-archive draft passes payload/outer checksums and independent byte inspection. Both macOS packages pass actual packaged installer execution/idempotence/uninstall and user-data preservation; foreign installers refuse before writes. Native Linux/Windows/Intel execution remains external. Final collection identities are recorded separately in distribution sidecars. |
| DOD-001 | Repeated first-key and no-jump behavior is demonstrated in deterministic/snapshot/PTY tests; actual emulator appearance is not proven by those checks. Final source must preserve the regressions. |
| DOD-002 | Durable saves remain asynchronous; only worker commit acknowledgement labels a result saved. Actual locked-store/retry/unsaved-exit workflows pass. The exact final production full suite validates169 processes and1017 saved results, including1000 restarts, with matched finalized/storage counts and ordinary input latency/active CPU below the stated local targets. |
| DOD-003 | Palette uses shared searchable setting metadata and default active surface is text plus timer. Snapshot/PTY interaction checks pass on recorded build; subjective actual-emulator review remains external. |
| DOD-004 | Every one of the312 requirements has a stable ledger row and a concrete individual audit. Frozen-source tests, cross-checks, actual measurements, archive/installer verification and the implementation commit are recorded. The central ledger distinguishes277 local/review passes from35 outstanding external validations, with no whole-goal completion claim. Distribution sidecars record final artifact verification without self-referential hashes. |
